---
description: 项目编码质量标准——文件/函数体积、测试要求、质量检查流程
alwaysApply: true
---

# 编码质量标准

## 质量检查（每次实现后执行）

```
go build ./...          # 编译通过
go vet ./...            # 静态检查
staticcheck ./...       # 扩展检查
go test ./... -count=1  # 全量测试
```

## 体积控制

| 指标 | 上限 | 超限处理 |
|------|------|----------|
| 单文件行数 | 300 行 | 按职责拆分 |
| 测试文件行数 | 500 行 | 按主题拆分 |
| 单函数行数 | 40 行 | 提取子函数 |
| 参数个数 | 5 个 | 使用结构体 |
| 嵌套层级 | 3 层 | 提前 return 或提取 |

## 纯函数优先

分离计算与副作用。IO 推到最外层，内部模块接收已解析的值。

## 测试要求

- 表驱动测试 + `t.Run`
- check 辅助函数封装断言
- 新增公开函数必须有测试
- bug 修复必须附带复现测试
- 用 `t.Setenv`、`t.TempDir` 隔离环境

## 注释与提交

- 关键注释使用中文，不写显而易见的注释
- 每完成独立功能提交 commit（`feat/fix/refactor: 简要描述`）
- 代码变更后同步检查 README 和 docs/ 是否需要更新
