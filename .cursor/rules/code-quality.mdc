---
description: 项目编码质量标准——文件/函数体积、测试要求、质量检查流程
alwaysApply: true
---

# 编码质量标准

## 质量检查（每次实现后执行）

```
go build ./...          # 编译通过
go vet ./...            # 静态检查
staticcheck ./...       # 扩展检查
go test ./... -count=1  # 全量测试
```

## 体积控制

| 指标 | 上限 | 超限处理 |
|------|------|----------|
| 单文件行数 | 300 行 | 按职责拆分 |
| 测试文件行数 | 500 行 | 按主题拆分 |
| 单函数行数 | 40 行 | 提取子函数 |
| 参数个数 | 5 个 | 使用结构体 |
| 嵌套层级 | 3 层 | 提前 return 或提取 |

## 纯函数优先

分离计算与副作用。IO 推到最外层，内部模块接收已解析的值。

## 测试要求

- 表驱动测试 + `t.Run`
- check 辅助函数封装断言
- 新增公开函数必须有测试
- bug 修复必须附带复现测试
- 用 `t.Setenv`、`t.TempDir` 隔离环境

## 文档同步（每次提交前检查）

代码变更后**必须**检查以下文档是否需要同步更新：

| 变更类型 | 需检查的文档 |
|----------|-------------|
| 函数签名/结构体变更 | 对应模块的 `spec/*.md` |
| 新增/删除配置项 | `spec/config.md`、`README.md` |
| 快捷键/交互变更 | `spec/selector.md`、`README.md` |
| 依赖版本变更 | `spec/dependencies.md` |
| 新增/删除文件 | `spec/architecture.md` |
| 错误处理/行为变更 | 对应模块的 `spec/*.md` |

如果变更影响了文档中描述的行为，文档更新应与代码变更在**同一个 commit** 中提交。

## 国际化（i18n）

所有用户可见文本必须通过 `i18n.Messages` 获取，禁止硬编码。覆盖范围：
- TUI 界面文本（selector、dialog）
- CLI 帮助文本和用法提示
- 命令错误消息
- 操作执行输出（clone、ship 等提示）
- Shell 安装消息

唯一例外：`config.go` 的配置解析错误（发生在 locale 解析之前）。

新增用户可见文本时：
1. 在 `i18n.Messages` 中添加字段
2. 在 `EN` 和 `ZH` 语言包中添加翻译
3. 通过 `msgs` 参数获取文本，不直接使用字符串字面量

详见 `spec/i18n.md`。

## 注释与提交

- 关键注释使用中文，不写显而易见的注释
- 每完成独立功能提交 commit（`feat/fix/refactor: 简要描述`）
