---
description: Go 编码风格——格式化、接口设计、错误处理
alwaysApply: true
---

# Go 编码风格

## 格式化

gofmt 和 goimports 是强制要求，不存在风格讨论空间。

## 设计原则

- 接受接口，返回结构体
- 保持接口精简（1-3 个方法）
- 在使用方定义接口，而非在实现方
- 让零值可用

## 错误处理

始终用上下文包装错误，使用 `%w` 保留错误链：

```go
if err != nil {
    return fmt.Errorf("创建用户失败: %w", err)
}
```

- 使用 `errors.Is` / `errors.As` 检查错误
- 不用 `_` 忽略错误（除非确实无关紧要）
- 不用 panic 做流程控制

## 函数设计

- 尽早返回，保持正常路径不缩进
- Context 作为第一个参数
- 避免裸返回（特别是长函数）
